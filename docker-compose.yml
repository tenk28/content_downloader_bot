services:
  bot:
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    build: .
    container_name: telegram-bot
    restart: unless-stopped  # auto-restart if it crashes
    env_file: .env           # load env vars from .env
